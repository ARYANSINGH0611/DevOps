stages:
  - test

test:
  image: python:3.8  # Choose an image with the required environment
  script:
    - pip install -r requirements.txt  # Install dependencies
    - pytest tests/test_google_search.py  # Run your tests
